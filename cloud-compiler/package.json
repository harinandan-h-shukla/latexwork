{
  "name": "inkwell-cloud-compiler",
  "version": "0.1.0",
  "description": "Multi-tenant LaTeX compile service — the real backend behind Inkwell's \"cloud\" compiler preference. Called only by the Next.js server (never end users directly). Runs each compile sandboxed (bwrap + prlimit) since it compiles untrusted users' documents, unlike local-agent which trusts its single owning user.",
  "private": true,
  "main": "dist/cli.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli.ts",
    "start": "node dist/cli.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.2",
    "tree-kill": "^1.2.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
